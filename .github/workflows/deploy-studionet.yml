name: Verify Studionet Deployment

on:
  push:
    paths:
      - ".github/workflows/deploy-studionet.yml"
  workflow_dispatch:

jobs:
  deploy-and-smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install -r requirements.txt
      - name: Verify live ProofJudge contract with GenLayer consensus
        run: gltest tests/integration/test_studionet_smoke.py -v -s --network studionet
